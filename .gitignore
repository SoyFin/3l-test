# --- Node.js / Next.js 标准忽略规则 ---

# 依赖文件夹 (970 MB 的罪魁祸首)
node_modules/

# Next.js 构建产物 (437 MB 的罪魁祸首)
.next/
out/

# 环境变量 (包含敏感信息，严禁上传)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 操作系统文件
.DS_Store
Thumbs.db

# IDE 配置
.idea/
.vscode/
*.swp
*.swo

# Prisma (通常只需要 schema.prisma，生成的客户端不需要上传，但为了保险先忽略生成的)
# 如果你的 prisma 文件夹里有生成的客户端，建议忽略，只保留 schema
prisma/generated/ 